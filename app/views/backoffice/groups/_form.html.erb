<%= form_for([:backoffice, @group]) do |f| %>
  <% if @group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group.errors.count, "error") %> prohibited this backoffice_group from being saved:</h2>

      <ul>
      <% @roup.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :logo_url %><br />
    <%= f.text_field :logo_url %>
  </div>
  <div class="field">
    <%= f.label :public_information %><br />
    <%= f.text_area :public_information %>
  </div>
  <div class="field">
    <%= f.label :private_information %><br />
    <%= f.text_area :private_information %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
