<%= form_for([@recurrence, @recurrence.comments.build]) do |f| %>  
  <%= f.text_area :body, :rows => 3, :class => 'text_field' %>
  <div class="actions">
    <%= f.submit :class => 'btn btn-primary' %>
    <%= link_to name = "", 'http://daringfireball.net/projects/markdown/syntax#place', html_options = { class: "icon-pencil", title: "Markdown::Syntax", target: "new" } %> Syntax 
  </div> 
<% end %>
