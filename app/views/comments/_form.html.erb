<%= form_for([@recurrence, @recurrence.comments.build]) do |f| %>  
  <div class="form-group">
    <%= f.text_area :body, :rows => 3, class: "form-control" %>
  </div> 
  <div class="form-group">
    <%= f.submit :class => 'btn btn-success btn-xs' %>
    <%= render "shared/markdown_link" %> 
  </div> 
<% end %>
