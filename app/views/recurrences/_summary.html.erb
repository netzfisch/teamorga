<dl>



  <br />

  <%= form_for([@recurrence, @recurrence.comments.build]) do |f| %>  
    <dt><strong>Hinzufügen:</strong></dt>
    <dd><%= f.text_area :body, :rows => 3, :class => 'text_field' %></dd>
    <div class="form-actions">
      <%= f.submit :class => 'btn btn-primary' %>
    </div> 
  <% end %>  

</dl> 
